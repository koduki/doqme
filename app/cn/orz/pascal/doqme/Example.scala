package cn.orz.pascal.doqme

object Example {

  /*
import cn.orz.pascal.doqme._

val url = "http://b.hatena.ne.jp/hotentry.rss"
val feeds = new FeedReader().read(url)

val favorites = Map("閉店" -> 5, "まとめ" -> 6, "理由" -> 7)
val features = FeatureAnalyzer.analyze(feeds(0))
val score = new ScoreCalcurator().calcurate(favorites)(features)
    */
}
